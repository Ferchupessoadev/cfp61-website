{% extends 'layouts/base.twig' %}

{% block title %}
    <title>CFP61 - Trayectos</title>
{% endblock %}

{% block content %}
    <section class="flex flex-col items-center justify-center min-h-[calc(100vh-64px)] bg-slate-800">
        <h1 class="text-4xl font-bold text-[var(--bg-primary)] p-4 rounded-md w-full px-14">
            Trayectos
        </h1>
				<div class="flex flex-col gap-0 md:gap-4 md:p-10">
					{% for course in courses %}
						<div class="flex flex-col md:flex-row gap-4 h-max w-full bg-[rgba(0,0,0,0.2)] p-4 rounded-lg">
							<img class="h-[300px] w-full md:w-1/3 object-cover" src="{{ course.image }}" alt="{{ course.name }}">
							<div class="flex flex-col w-full md:w-2/3 gap-4">
								<h2 class="text-2xl font-bold text-white">
									{{ course.name }}
								</h2>
								<p class="text-md text-white">
									{{ course.description }}
								</p>
								<div class="mt-auto pt-10 flex gap-4">
									<a class="text-white bg-blue-700 w-max hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center" href="/trayectos/{{ course.name }}">Leer más</a>
									<a class="text-white bg-blue-700 w-max hover:bg-blue-800 focus:ring-4 focus:outline-none focus:ring-blue-300 font-medium rounded-lg text-sm px-5 py-2.5 text-center" href="/preinscripcion/{{ course.name }}">Pre inscribirse</a>
								</div>
							</div>
						</div>
					{% else %}
			
					{% endfor %}
				</div>
    </section>
{% endblock %}
