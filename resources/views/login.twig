{% extends 'layouts/base.twig' %}

{% block title %}
    <title>Login</title>
{% endblock %}

{% block content %}
    <section class="flex flex-col justify-center items-center h-screen bg-[linear-gradient(to_right,#f0f9ff,#a8c0ff)]">
        <form class="flex flex-col items-center gap-4 bg-slate-900 shadow-2xl md:rounded px-4 py-6 mb-4 w-full md:w-1/2" action="/login" method="post">
            <img class="h-16" src="/img/logo.png" alt="Centro de formación profesional de la Criolla">
            <input class="px-4 py-2 mb-4 w-full border-none outline-none rounded-md" type="email" name="email" placeholder="Correo electronico">
            <input class="px-4 py-2 mb-4 w-full border-none outline-none rounded-md" type="password" name="password" placeholder="Contraseña">
						<a href="/recoverpassword" class="transition duration-100 ease-in-out text-sm md:text-lg mb-4 w-max cursor-pointer text-blue-500 border-b-2 border-blue-500">Recuperar contraseña</a>
            <input 
							class="transition duration-300 ease-in-out rounded-md px-4 py-2 mb-4 border-2 text-sm md:text-xl w-max cursor-pointer text-white hover:text-slate-900 hover:bg-gray-100" 
							type="submit" 
							value="Iniciar sesion">
        </form>
				<div class="flex flex-col items-center">
					<p class="text-black flex flex-col gap-2 items-center"><span>IP de conexión</span><span>{{ ip }}</span></p>
				</div>
    </section>
{% endblock %}
